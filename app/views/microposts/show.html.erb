<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @micropost.content %>
</p>

<p>
	<strong>User:</strong>

	<% User.where(id: [@micropost.user_id]).each do |user| %>
	
		<%= user.name %>
	
	<% end %>

</p>

<%= link_to 'Edit', edit_micropost_path(@micropost) %> |
<%= link_to 'Back', microposts_path %>
